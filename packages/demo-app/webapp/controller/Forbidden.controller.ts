import Controller from "sap/ui/core/mvc/Controller";

/**
 * @namespace demo.app.controller
 */
export default class ForbiddenController extends Controller {
	onInit(): void {
		// This controller should never be reached due to the guard
	}
}
